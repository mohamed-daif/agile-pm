apiVersion: v1
kind: ConfigMap
metadata:
  name: agile-pm-config
  namespace: agile-pm
data:
  LOG_LEVEL: "INFO"
  LOG_FORMAT: "json"
  API_HOST: "0.0.0.0"
  API_PORT: "8000"
  API_PREFIX: "/api/v1"
  TRACING_ENABLED: "true"
  TRACING_SAMPLE_RATE: "0.1"
  METRICS_ENABLED: "true"
